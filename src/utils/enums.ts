export enum VaultButton__Enum {
  TEXT, // 0
  COLOR, // 1
  DISABLED, // 2
  CURSOR, // 3
  PINK_BUTTON, // 4
}

export enum OptionType {
  LONG_CALL,
  LONG_PUT,
  SHORT_CALL_BASE, // base collateral
  SHORT_CALL_QUOTE, // quote collateral
  SHORT_PUT_QUOTE, // quote collateral
}

export enum CloseSelectedButton__Enum {
  DISABLED,
  BOX_SHADOW,
  BORDER_COLOR,
  CURSOR,
  COLOR,
  X_ICON,
}

export enum CancelCloseSelectedButton__Enum {
  DISABLED,
  BOX_SHADOW,
  BORDER_COLOR,
  CURSOR,
  COLOR,
}
